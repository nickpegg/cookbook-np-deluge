machine:
  services:
    - docker
  ruby:
    version: 2.3.1

test:
  override:
    - bundle exec rubocop
    - bundle exec rspec --color --deprecation-out /dev/null
    # - bundle exec kitchen test -c4
